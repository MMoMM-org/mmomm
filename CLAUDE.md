# MMoMM.org — Project Claude Config

## Project Conventions
- Use relative paths (`./content/...`, `./static/...`) in all docs and workflow files — never absolute paths. Include "Wechsele in die Repo-Root" instruction before commands.
